import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const Home = () => {
  return (
    <View >
      <Text style={{ justifyContent:'center',alignItems: 'center', fontSize: 24, fontWeight: 'bold'}}>We are in Home screen what would you like to do</Text>
    </View>
  )
}

export default Home

const styles = StyleSheet.create({})